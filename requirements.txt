# Core runtime has NO required third-party dependencies (stdlib only).
# Install extras for full functionality:
#
#   pip install -e ".[llm]"    # Claude-based LLM review (Anthropic SDK)
#   pip install -e ".[aws]"    # real AWS mode: SQS queue + boto3
#   pip install -e ".[dns]"    # robust CNAME resolution for subdomain-takeover checks
#   pip install -e ".[dev]"    # pytest
#
# Pinned set for a typical deployment:
anthropic>=0.40
boto3>=1.34
dnspython>=2.4
